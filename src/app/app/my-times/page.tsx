/**
 * My Times Page — /app/my-times
 *
 * Employee self-service: view own time entries and overtime with date range.
 */

"use client";

import { EmployeeSelfService } from "@/components/employee-self-service";

export default function MyTimesPage() {
  return <EmployeeSelfService />;
}
